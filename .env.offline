# Gebruikt door `npm run dev:local` (vite --mode offline): overschrijft de
# Supabase-variabelen uit .env.local met leeg, zodat de app zonder database
# draait — plantendata uit data/, tuin alleen in de browser. Geen geheimen,
# mag in git.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
